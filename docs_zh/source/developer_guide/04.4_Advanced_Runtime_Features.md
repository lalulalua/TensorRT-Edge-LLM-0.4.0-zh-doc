# 高级运行时特性

## 概览

TensorRT Edge-LLM C++ 运行时提供多项高级能力：从 CUDA Graph 优化到动态 LoRA 适配切换，面向生产环境提升性能、灵活性与效率。

---

## CUDA Graph 优化

运行时为**生成阶段**提供 CUDA Graph 捕获与执行（**仅标准模式**）：

**图捕获流程**：

- **预执行**：捕获前执行一次 TensorRT 引擎，避免错误
- **状态模拟**：模拟 KV 缓存状态以匹配 prefill 之后情形
- **输入校验**：捕获前校验张量形状与配置
- **图创建**：CUDA 流捕获记录整步生成执行
- **按哈希存储**：按输入形状与 LoRA 配置生成键存储图

**图执行**：

- **哈希查找**：对输入配置哈希以查找已捕获图
- **直接启动**：匹配则经 `cudaGraphLaunch` 启动
- **回退**：不匹配则回退标准 TensorRT 执行
- **多配置**：不同批大小与 LoRA 分别捕获

**性能收益**：

- **降低内核启动开销**：约可减少 10–30%
- **延迟更稳**：逐词元延迟更可预期
- **显存访问**：重放优化访问模式

**限制**：

- **仅标准运行时**：EAGLE SpecDecode 模式不支持 CUDA Graph
- **与配置绑定**：不同批大小与 LoRA 需分别捕获
- **显存开销**：每份捕获图占用额外 GPU 显存

---

## LoRA（低秩适配）支持

**LoRA（Low-Rank Adaptation）** 通过低秩分解矩阵适配大模型，而非更新全部权重；在现有层上叠加小型可训练矩阵，以较低显存与计算成本实现任务定制。

运行时提供完整的动态 LoRA 管理：

**适配器管理**：

- **SafeTensors 加载**：自标准 SafeTensors 加载权重
- **动态注册**：按名称管理多适配器
- **秩校验**：与引擎支持的最大秩对齐
- **GPU 存储**：高效分配适配权重显存

**运行时切换**：

- **零秩关闭**：将秩维置 0 可关闭适配器
- **张量绑定**：LoRA 张量绑定到 prefill 与生成执行上下文
- **CUDA Graph**：每种 LoRA 配置单独捕获图
- **状态保持**：切换适配器时保留 KV 与模型状态

**API 接口**：

- **`addLoraWeights(name, filePath)`**：从 SafeTensors 加载
- **`switchLoraWeights(name)`**：切换激活适配器；空名为关闭
- **`getAvailableLoraWeights()`**：已加载列表
- **`getActiveLoraWeightsName()`**：当前激活名称

**适用场景**：

- **领域适配**：医疗、法律、技术等场景切换
- **多租户**：为不同用户提供不同定制
- **A/B 测试**：对比微调变体
- **任务优化**：按任务选用专用适配器

---

## 批处理

推理流水线支持高效批处理：

**显存管理**：

- **统一分配**：按最大批大小预留批感知显存
- **张量布局**：支持多序列并行的一致布局
- **动态填充**：序列填充到批内最大长度并行计算

**执行流程**：

- **并行 Prefill**：prefill 阶段同时处理批内序列
- **并发生成**：每轮为所有活跃序列生成词元
- **独立跟踪**：每序列独立完成状态
- **动态移除**：完成的序列从批中移除

**收益**：

- **提高吞吐**：同时处理多请求
- **GPU 利用率**：并行更好利用 GPU
- **摊销开销**：固定成本由多序列分摊

**注意**：

- **显存**：批大小受 GPU 显存限制
- **延迟权衡**：批变大可能增加单请求延迟

---

## 系统提示 KV 缓存优化

对重复系统提示实现智能缓存：

**缓存管理**：

- **哈希存储**：对「系统提示 + LoRA 适配」联合哈希
- **KV 持久化**：相同系统提示复用 KV 内容
- **省显存**：避免重复 prefill
- **自动复用**：匹配则自动加载缓存

**收益**：

- **降低延迟**：缓存命中时省去 prefill 计算
- **显存优化**：高频提示状态紧凑存储
- **与批兼容**：可与批处理协同

**场景**：

- **固定指令的聊天机器人**
- **API 服务**：多用户复用系统提示
- **多轮对话**：跨轮复用上下文

---

## 词表压缩

词表压缩通过选取与领域最相关的子集，缩小模型规模并加速推理。

**概览**：

- **词元选择**：使用 `tensorrt-edgellm-reduce-vocab` 生成映射（实现见 `tensorrt_edgellm/vocab_reduction/vocab_reduction.py`）
- **运行时透明**：引擎目录存在 `vocab_map.safetensors` 时自动使用
- **收益**：更小 LM head、更快推理、更低显存

**方法**：`input_aware`（推荐，分析用法）或 `frequency`（按频率）

**说明**：词表压缩与任务强相关，所给方法仅为参考；用户应结合自有方法与样本数据构建映射，以覆盖预期词元。

---

## 多模态处理

运行时全面支持视觉语言模型（VLM）：

**视觉流水线**：

- **图像预处理**：归一化、缩放与张量转换
- **ViT 执行**：ViT 生成视觉嵌入
- **词元融合**：视觉嵌入在 LLM 处理前与文本融合
- **动态分辨率**：可变分辨率与 patch 数量

**支持架构**：

- **Qwen-VL 系列**：Qwen2/2.5/3-VL，动态 patch 与窗口注意力
- **InternVL 系列**：InternVL3，0.5 下采样率与固定尺寸处理
- **Phi-4-Multimodal**：基于 LoRA 的视觉语言支持
- **旋转位置编码**：多模态序列的位置编码

**处理流程**：

1. 从请求路径加载图像
2. ViT 编码得到视觉嵌入
3. 与文本词元嵌入合并
4. 经语言模型处理
5. 基于多模态上下文生成文本

**性能特点**：

- **Prefill 影响**：视觉处理增加 prefill 延迟
- **显存**：视觉嵌入提高 KV 需求
- **批处理**：可多图批处理

---

## 使用示例

### 使用 CUDA Graph

```cpp
// CUDA graphs are automatically enabled for standard runtime
LLMInferenceRuntime runtime(engineDir);

// First inference captures the graph
auto response1 = runtime.handleRequest(request);

// Subsequent inferences with same configuration use captured graph
auto response2 = runtime.handleRequest(request);
```

### 动态 LoRA 切换

```cpp
// Load multiple LoRA adapters
runtime.addLoraWeights("medical", "lora_weights/medical_adapter.safetensors");
runtime.addLoraWeights("legal", "lora_weights/legal_adapter.safetensors");

// Switch between adapters dynamically
runtime.switchLoraWeights("medical");
auto medical_response = runtime.handleRequest(medical_request);

runtime.switchLoraWeights("legal");
auto legal_response = runtime.handleRequest(legal_request);

// Disable LoRA to use base model
runtime.switchLoraWeights("");
auto base_response = runtime.handleRequest(base_request);
```

### 批处理

```cpp
// Prepare multiple requests
std::vector<InferenceRequest> batch_requests = {
    {.inputText = "Question 1", .maxLength = 100},
    {.inputText = "Question 2", .maxLength = 100},
    {.inputText = "Question 3", .maxLength = 100}
};

// Process batch (implementation depends on your integration)
for (const auto& request : batch_requests) {
    auto response = runtime.handleRequest(request);
    // Process response...
}
```

### 系统提示缓存

```cpp
// First request with system prompt - triggers cache creation
InferenceRequest request1;
request1.systemPrompt = "You are a helpful medical assistant.";
request1.inputText = "What is aspirin?";
auto response1 = runtime.handleRequest(request1);

// Second request with same system prompt - reuses cache
InferenceRequest request2;
request2.systemPrompt = "You are a helpful medical assistant.";
request2.inputText = "What is ibuprofen?";
auto response2 = runtime.handleRequest(request2);  // Faster prefill!
```

### 多模态 VLM 推理

```cpp
// Initialize runtime with visual engine
LLMInferenceRuntime runtime(engineDir, visualEngineDir);

// Single image inference
InferenceRequest request1;
request1.inputText = "Describe this image in detail.";
request1.imagePaths = {"photo.jpg"};
auto response1 = runtime.handleRequest(request1);

// Multiple images
InferenceRequest request2;
request2.inputText = "Compare these two images.";
request2.imagePaths = {"image1.jpg", "image2.jpg"};
auto response2 = runtime.handleRequest(request2);
```

### 词表压缩

```bash
# Step 1: Reduce vocabulary using input-aware analysis
tensorrt-edgellm-reduce-vocab \
  --model_dir Qwen/Qwen3-4B-Instruct-2507 \
  --output_dir reduced_vocab \
  --reduced_vocab_size 16384 \
  --method input_aware \
  --max_samples 100000
  # Optional: Add --d2t_path for EAGLE speculative decoding models
  # --d2t_path onnx_models/qwen3-4b_eagle_draft/d2t.safetensors

# Step 2: Export model with reduced vocabulary
tensorrt-edgellm-export-llm \
  --model_dir quantized/qwen3-4b \
  --output_dir llm_onnx \
  --reduced_vocab_dir reduced_vocab/

# Step 3: Build TensorRT engine (same as standard workflow)
./build/examples/llm/llm_build \
  --onnxDir llm_onnx \
  --engineDir engines/qwen3-4b \
  --maxBatchSize 1

# Step 4: Run inference (same as standard workflow)
# Runtime automatically uses vocab_map.safetensors when present
./build/examples/llm/llm_inference \
  --engineDir engines/qwen3-4b \
  --inputFile input.json \
  --outputFile output.json
```

**输出**：`reduced_vocab/` 目录含 `vocab_map.safetensors`，推理时由运行时自动应用。

---

## 下一步

1. **运行示例**：见 [示例](05_Examples.md)
2. **基准测试**：评估 CUDA Graph、LoRA、批处理带来的影响
3. **集成应用**：用高级特性优化部署
4. **API 文档**：见 `cpp/runtime/` 头文件

---

## 更多资料

- **运行时 API**：见 `cpp/runtime/` 目录
- **示例应用**：见 `examples/llm/` 与 `examples/multimodal/`
- **架构概览**：见 [概览](01.1_Overview.md)
- **LoRA**：见 Python 导出流水线中的 LoRA 说明
